@page "/raids"

@if(raids==null) {
    <h3>Loading raids...</h3>
} else {
    <NewRaidForm Raids="@this" />
    @foreach (var raid in raids)
    {
        <RaidForm Raid="@raid" Players="@players" Instances="@instances" Approvals="@approvals" Raids="@this" />
    }
}